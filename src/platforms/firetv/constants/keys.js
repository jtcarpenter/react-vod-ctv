import * as keyTypes from 'constants/keyTypes';

export const keys = {
    37: keyTypes.KEY_LEFT,
    38: keyTypes.KEY_UP,
    39: keyTypes.KEY_RIGHT,
    40: keyTypes.KEY_DOWN,
    13: keyTypes.KEY_OK,
    179: keyTypes.KEY_PLAY_PAUSE,
    8: keyTypes.KEY_BACK
}